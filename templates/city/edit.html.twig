{% extends 'base.html.twig' %}

{% block title %}Éditer ville
{% endblock %}

{% block body %}
	<div class="container mx-auto px-4 py-6">
		<div class="content-container max-w-4xl mx-auto">

			<div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-8 gap-4">
				<div>
					<h1 class="text-3xl font-bold text-gray-800 mb-2">
						Éditer la ville
					</h1>
					<p class="text-gray-600">Modifiez les informations de la ville et ses frais de livraison</p>
				</div>
			</div>

			{% include 'layouts/_flash_message.html.twig' %}

			<div class="card bg-white">
				<div class="">
					{{ form_start(form) }}
					
					<div class="space-y-6">
						<div class="mt-4">
							{{ form_label(form.name, 'Nom de la ville', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
							{{ form_widget(form.name, {'attr': {'class': 'input-style input-style-common', 'placeholder': 'Saisissez le nom de la ville'}}) }}
							<div class="m-1 text-red-500 text-sm">
								{{ form_errors(form.name) }}
							</div>
						</div>

						<div>
							{{ form_label(form.shipping_cost, 'Frais de livraison (€)', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'}}) }}
							{{ form_widget(form.shipping_cost, {'attr': {'class': 'input-style input-style-common', 'placeholder': 'Saisissez les frais de livraison en euros'}}) }}
							<div class="m-1 text-red-500 text-sm">
								{{ form_errors(form.shipping_cost) }}
							</div>
						</div>

						<!-- Boutons d'action -->
						<div class="border-t border-gray-200 pt-6 mt-12">
							<div class="flex flex-col sm:flex-row gap-4 justify-end">
								<a href="{{ path('app_city') }}" class="custom-btn-with-logo back-btn">
									<ion-icon name="close-outline"></ion-icon>
									Annuler
								</a>
								<button type="submit" class="custom-btn-with-logo edit-btn">
									<ion-icon name="checkmark-outline"></ion-icon>
									Mettre à jour
								</button>
							</div>
						</div>
					</div>

					{{ form_end(form) }}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
