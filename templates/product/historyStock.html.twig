{% extends 'base.html.twig' %}

{% block title %}Historique du stock
{% endblock %}

{% block body %}
	<div class="container mx-auto px-4 py-6">
		<div
			class="content-container">
			{# En-tête avec titre et bouton de retour #}
			<div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-8 gap-4">
				<div>
					<h1 class="text-3xl font-bold text-gray-800 mb-2">
						Historique du stock
					</h1>
					<p class="text-gray-600">Historique des ajouts de stock</p>
				</div>
				<a href="{{ path('app_product') }}" class="custom-btn-with-logo back-btn">
					<ion-icon name="return-down-back-outline"></ion-icon>
					Retour
				</a>
			</div>

			{# Messages flash #}
			{% include 'layouts/_flash_message.html.twig' %}

			{# Tableau de l'historique #}
			<div class="card bg-white">
				<div class="card-body p-0">
					{% if productsAdded|length > 0 %}
						<div class="overflow-x-auto">
							<table class="table table-zebra w-full border-2 border-base-300">
								<thead class="bg-gray-300">
									<tr>
										<th class="text-center py-4 px-2 sm:px-6 border-r border-base-300">ID</th>
										<th class="py-4 px-2 sm:px-6 border-r border-base-300">Produit</th>
										<th class="py-4 px-2 sm:px-6 border-r border-base-300">Quantité ajoutée</th>
										<th class="text-center py-4 px-2 sm:px-6">Date d'ajout</th>
									</tr>
								</thead>
								<tbody>
									{% for productHistory in productsAdded %}
										<tr class="hover:bg-base-50 transition-colors duration-200">
											<td class="text-center py-4 px-2 sm:px-6 border-r-2 border-base-300">{{ productHistory.id }}</td>
											<td class="py-4 px-2 sm:px-6 border-r-2 border-base-300 font-bold">{{ productHistory.product.name }}</td>
											<td class="py-4 px-2 sm:px-6 border-r-2 border-base-300">+{{ productHistory.quantity }}</td>
											<td class="text-center py-4 px-2 sm:px-6">{{ productHistory.createdAt|date('d/m/Y H:i') }}</td>
										</tr>
									{% endfor %}
								</tbody>
							</table>
						</div>
					{% else %}
						<div class="text-center py-16">
							<ion-icon name="archive-outline" class="text-6xl text-gray-400 mb-4"></ion-icon>
							<h3 class="text-xl font-semibold text-gray-600 mb-2">Aucun historique</h3>
							<p class="text-gray-500">Aucun ajout de stock n'a été enregistré</p>
						</div>
					{% endif %}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
